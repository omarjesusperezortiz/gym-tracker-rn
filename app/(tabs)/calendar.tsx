import { PlaceholderScreen } from '@/src/components/PlaceholderScreen';

export default function CalendarScreen() {
  return (
    <PlaceholderScreen
      emoji="📅"
      title="Calendar"
      body="Month view, streaks, and skate/rest logging are coming in a later phase."
    />
  );
}
