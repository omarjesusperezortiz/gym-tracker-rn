import { PlaceholderScreen } from '@/src/components/PlaceholderScreen';

export default function ProgressScreen() {
  return (
    <PlaceholderScreen
      emoji="📈"
      title="Progress"
      body="Per-exercise top-set charts are coming in a later phase."
    />
  );
}
